# Sentry configuration file
defaults.url=https://sentry.io/
defaults.org=dinnermatch
defaults.project=dinnermatch-backend

# Authentication token (set via environment variable SENTRY_AUTH_TOKEN)
# auth.token=your-sentry-auth-token

# CLI configuration
cli.executable=sentry-cli